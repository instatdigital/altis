import Foundation

/// Typed route definitions for top-level app navigation.
/// Placeholder — extended in Phase 4 when feature flow contracts are defined.
enum AppRoute: Hashable {
    case home
    case project
    case board
    case taskList
    case kanbanBoard
    case taskPage
}
