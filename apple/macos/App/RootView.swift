import SwiftUI

struct RootView: View {
    var body: some View {
        Text("Altis macOS bootstrap")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .padding(24)
    }
}

#Preview {
    RootView()
}
